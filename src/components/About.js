import React from 'react'

const About = () => {
  // const a = useContext(noteContext);
  // useEffect(() => {
  //   a.update();
  // }, [])

  return (
    <div>About component</div>
  )
}

export default About
